---
description: Approve a queued gate card.
argument-hint: <path/to/inbox/file.md>
---

Approve the gate card at the given path:

```bash
atelier inbox approve "$ARGUMENTS"
```

Report which gate is now unblocked.
